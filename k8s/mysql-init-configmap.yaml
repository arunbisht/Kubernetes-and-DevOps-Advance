apiVersion: v1
kind: ConfigMap
metadata:
  name: mysql-init-configmap
  namespace: employee-app
data:
  init.sql: |
    CREATE DATABASE IF NOT EXISTS employee_db;
    USE employee_db;

    CREATE TABLE IF NOT EXISTS employees (
      id INT AUTO_INCREMENT PRIMARY KEY,
      name VARCHAR(100) NOT NULL,
      department VARCHAR(100) NOT NULL,
      designation VARCHAR(100) NOT NULL,
      salary DECIMAL(10, 2) NOT NULL,
      email VARCHAR(150) UNIQUE NOT NULL
    );

    INSERT INTO employees (name, department, designation, salary, email) VALUES
      ('Arun Bisht',    'Engineering',  'Frontend Developer',  75000, 'arun@example.com'),
      ('Priya Sharma',  'Engineering',  'Backend Developer',   80000, 'priya@example.com'),
      ('Rahul Gupta',   'DevOps',       'DevOps Engineer',     85000, 'rahul@example.com'),
      ('Neha Verma',    'Design',       'UI/UX Designer',      70000, 'neha@example.com'),
      ('Amit Kumar',    'Engineering',  'Full Stack Developer', 90000, 'amit@example.com'),
      ('Sunita Patel',  'HR',           'HR Manager',          65000, 'sunita@example.com'),
      ('Vikram Singh',  'Finance',      'Financial Analyst',   72000, 'vikram@example.com'),
      ('Deepa Nair',    'Engineering',  'QA Engineer',         68000, 'deepa@example.com');
